# Expense App

This repository serves as the **main application workspace**.
It contains the following sub-projects organized inside the `expense-app` folder:

* **backend-app** – Handles server-side logic, APIs, and database integration.
* **mobile-app** – A cross-platform mobile application built with Expo/React Native.
* **web-app** – A web client for managing and visualizing expenses.

